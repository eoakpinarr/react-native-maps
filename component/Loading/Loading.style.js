import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        position: "absolute",
        backgroundColor: 'white',
        alignSelf: 'center',
        padding: 10,
        borderWidth: 1,
        borderColor: '#e0e0e0',
        borderRadius: 10,
        bottom: 200
    }
})